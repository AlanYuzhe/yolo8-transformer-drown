# dataset.yaml 配置文件
path: /project/ece601/ec601_lifeguard  # 数据集根目录
train: image  # 训练图像目录 (相对于 'path')
val: image  # 验证图像目录 (相对于 'path')
test:  # 测试图像目录 (可选, 相对于 'path')

# 类别
names:
  0: drowning
  1: normal
